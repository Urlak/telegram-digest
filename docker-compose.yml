version: '3.8'
services:
  telegram-digest:
    build: .
    container_name: telegram-digest
    restart: unless-stopped
    ports:
      - "8041:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
